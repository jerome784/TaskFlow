bucket         = "taskflow-terraform-state-prod" # Create this S3 bucket first
key            = "taskflow/prod/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
# dynamodb_table = "terraform-lock-table" # Optional: Create for state locking
